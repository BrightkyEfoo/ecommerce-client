version: '4'
name: next-js-server
services:
    app:
        build:
            context: .
        container_name: client
        env_file:
            -   .env
        ports:
            -   '3000:3000'
        environment:
            - NODE_ENV=production
        